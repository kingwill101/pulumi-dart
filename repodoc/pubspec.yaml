name: repodoc
description: Repository maintenance, generation, and diagnostics for Pulumi Dart.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  artisanal: ^0.5.0
  crypto: ^3.0.7
  path: ^1.9.1
  pub_semver: ^2.2.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.29.0

executables:
  repodoc:
