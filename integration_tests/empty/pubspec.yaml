name: emptydart
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: ^3.5.1

# Add regular dependencies here.
dependencies:
  path: ^1.8.0
  pulumi:
    path: ../../pulumi-dart

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0

executables:
  main:
