name: aliases_rename
description: A Dart integration test fixture for alias rename.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  pulumi:
    path: ../../../../pulumi-dart

dev_dependencies:
  lints: ^6.1.0

