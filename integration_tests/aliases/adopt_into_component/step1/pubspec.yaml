name: aliases_adopt_into_component
description: A Dart integration test fixture for alias adoption into components.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  pulumi:
    path: ../../../../pulumi-dart

dev_dependencies:
  lints: ^6.1.0
