# Dart Random Template

Local usage:

```bash
pulumi new ./templates/dart-random -y --name my-random-stack --stack dev --secrets-provider passphrase
```

Remote usage:

```bash
pulumi new https://github.com/pulumi/pulumi-dart/tree/main/templates/dart-random -y --name my-random-stack --stack dev --secrets-provider passphrase
```
