name: dart_random
description: Pulumi Dart template using random provider package
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  pulumi: ^1.0.0
  pulumi_random:
    git:
      url: https://github.com/pulumi/pulumi-dart.git
      path: packages/random
