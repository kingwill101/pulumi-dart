# buf.gen.yaml
version: v2
managed:
  enabled: true

plugins:
  - remote: buf.build/protocolbuffers/dart:v21.1.2
    out: pulumi-dart/lib/src/pulumirpc
    opt:
      # Enable the "grpc" option if you need gRPC stubs.
      - grpc
    include_imports: true
