name: azure_native_dart_call_azure_api
description: Pulumi Azure Native example integrating provider auth with direct Azure REST API lookups.
version: 0.0.1
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  pulumi: ^3.0.0
  pulumi_azure_native: 3.27.0+1
  http: ^1.5.0

dev_dependencies:
  lints: ^6.1.0
