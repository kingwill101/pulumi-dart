name: azure_native_dart_credential_rotation_one_set
description: Pulumi Azure Native credential rotation one-set example in Dart.
version: 0.0.1
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  pulumi: 3.0.0
  pulumi_azure_native: 3.26.0
  pulumi_random: 4.21.1

dev_dependencies:
  lints: ^6.1.0
