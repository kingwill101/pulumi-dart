name: azure_native_dart_minecraft_server
description: Minecraft server example on Azure VM with Pulumi Dart.
version: 0.0.1
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  pulumi: ^3.0.0
  pulumi_azure_native: 3.27.0+1
  pulumi_command: 1.2.1+1

dev_dependencies:
  lints: ^6.1.0
