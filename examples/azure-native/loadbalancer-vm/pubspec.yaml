name: azure_native_dart_loadbalancer_vm
description: Pulumi Azure Native example deploying a load balancer and VM.
version: 0.0.1
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  pulumi: ^3.0.0
  pulumi_azure_native: 3.26.0+1
  pulumi_random: 4.21.1+1

dev_dependencies:
  lints: ^6.1.0
