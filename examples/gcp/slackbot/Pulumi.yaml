name: gcp-dart-slackbot
runtime: dart
description: Dart program creating Slackbot infrastructure on GCP
template:
  config:
    gcp:region:
      description: The GCP region to deploy into
      default: us-central1
