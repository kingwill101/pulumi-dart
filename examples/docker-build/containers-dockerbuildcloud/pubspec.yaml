name: docker_build_containers_dockerbuildcloud
description: Pulumi Docker Build Cloud + AWS Fargate example in Dart.
version: 0.0.1
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  pulumi: 3.0.0
  pulumi_aws: 7.20.0
  pulumi_awsx: 3.2.1
  pulumi_docker_build: 0.0.14

dev_dependencies:
  lints: ^6.1.0
