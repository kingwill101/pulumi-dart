name: openstack_dart_webserver
description: Pulumi OpenStack webserver example in Dart.
version: 0.0.1
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  pulumi: 3.0.0
  pulumi_openstack: 5.4.1

dev_dependencies:
  lints: ^6.1.0
