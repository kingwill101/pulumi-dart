name: aws_dart_esc_external_adapter_lambda
description: Pulumi ESC external adapter AWS Lambda example in Dart.
version: 0.0.1
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"
dependencies:
  pulumi: ^3.0.0
  pulumi_aws: ^7.20.0
  pulumi_aws_apigateway: 3.0.0+1

dev_dependencies:
  lints: ^6.1.0
