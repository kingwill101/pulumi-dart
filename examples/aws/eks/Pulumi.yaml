name: aws-dart-eks
description: EKS cluster example
runtime:
  name: dart
  options:
    resolution: workspace
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-west-2
