name: aws-dart-apigateway-eventbridge
runtime:
  name: dart
  options:
    resolution: workspace
description: A minimal AWS Dart Pulumi program
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-west-2
