name: aws-dart-s3-folder-component
runtime:
  name: dart
  options:
    resolution: workspace
description: A static website hosted on AWS S3
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-west-2
