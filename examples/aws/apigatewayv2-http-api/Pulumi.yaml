name: aws-dart-apigatewayv2-http-api
runtime:
  name: dart
  options:
    resolution: workspace
description: Example of using AWS API Gateway v2 HTTP API
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-2
