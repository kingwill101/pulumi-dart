name: aws_dart_django_voting_app
description: Voting app using Django and MySQL on ECS.
version: 0.0.1
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"
dependencies:
  pulumi: ^3.0.0
  pulumi_aws: ^7.20.0
  pulumi_docker: 5.1.0+1
  pulumi_mysql: 3.3.1+1

dev_dependencies:
  lints: ^6.1.0
