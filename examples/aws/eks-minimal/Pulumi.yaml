name: aws-dart-eks-minimal
description: A minimal example of using pulumi-eks
runtime:
  name: dart
  options:
    resolution: workspace
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
