name: aws_dart_lambda_slack
description: Pulumi AWS Lambda Slack webhook example.
version: 0.0.1
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"
dependencies:
  pulumi: ^3.0.0
  pulumi_aws: ^7.20.0

dev_dependencies:
  lints: ^6.1.0
