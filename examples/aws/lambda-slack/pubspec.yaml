name: aws_dart_lambda_slack
description: Pulumi AWS Lambda Slack webhook example.
version: 0.0.1
resolution: workspace

environment:
  sdk: ^3.11.0
dependencies:
  pulumi: 0.1.0
  pulumi_aws: any

dev_dependencies:
  lints: ^6.1.0