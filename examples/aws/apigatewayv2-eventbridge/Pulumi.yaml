name: aws-dart-apigatewayv2-eventbridge
runtime:
  name: dart
  options:
    resolution: workspace
description: An example that integrates API Gateway, EventBridge, and Lambda.
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-2
