name: aws-dart-lambda
runtime:
  name: dart
  options:
    resolution: workspace
description: Basic example of an AWS lambda
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
