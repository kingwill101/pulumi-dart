name: aws_dart_webserver
description: Pulumi AWS web server example.
version: 0.0.1
resolution: workspace

environment:
  sdk: ^3.11.0
dependencies:
  pulumi: 0.1.0
  pulumi_aws: any

dev_dependencies:
  lints: ^6.1.0