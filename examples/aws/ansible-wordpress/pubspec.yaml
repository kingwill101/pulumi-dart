name: aws_dart_ansible_wordpress
description: Deploy an EC2 Wordpress server using Pulumi and Ansible.
version: 0.0.1
resolution: workspace

environment:
  sdk: ^3.11.0
dependencies:
  pulumi: 0.1.0
  pulumi_aws: any
  pulumi_command: any

dev_dependencies:
  lints: ^6.1.0