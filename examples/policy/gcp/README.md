# GCP Policy Pack (Dart)

This example is a Dart port of `policy-packs/gcp-ts` from
`thirdparty/pulumi_examples/policy-packs/gcp-ts/`.

Run from this directory:

```bash
pulumi policy validate --policy-pack .
```
