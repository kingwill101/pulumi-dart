# Kubernetes Policy Pack (Dart)

This example is a Dart port of `policy-packs/kubernetes-ts` from
`thirdparty/pulumi_examples/policy-packs/kubernetes-ts/`.

Run from this directory:

```bash
pulumi policy validate --policy-pack .
```
