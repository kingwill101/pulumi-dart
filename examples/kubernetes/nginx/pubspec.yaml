name: kubernetes_dart_nginx
description: Pulumi Kubernetes Nginx deployment example in Dart.
version: 0.0.1
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  pulumi: ^3.0.0
  pulumi_kubernetes: 4.33.0+1

dev_dependencies:
  lints: ^6.1.0
