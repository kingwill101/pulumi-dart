name: kubernetes_dart_helm_release_wordpress
description: Pulumi Kubernetes Helm release WordPress example in Dart.
version: 0.0.1
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  pulumi: ^3.0.0
  pulumi_kubernetes: 4.34.0+1

dev_dependencies:
  lints: ^6.1.0
