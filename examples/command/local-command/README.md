# Local Command (Dart)

This is a Dart-first example using `pulumi_command` local `Command` resource.

## Deploy

```bash
pulumi up
```

## Outputs

- `stdout`
- `stderr`

## Destroy

```bash
pulumi destroy --yes
```
