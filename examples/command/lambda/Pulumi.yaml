name: command-dart-lambda
description: Pulumi Command lambda build example in Dart.
runtime:
  name: dart
  options:
    resolution: workspace
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-2
