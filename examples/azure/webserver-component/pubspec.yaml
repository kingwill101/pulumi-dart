name: azure_dart_webserver_component
description: Web server component using Azure Virtual Machines in Dart.
version: 0.0.1
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  pulumi: ^3.0.0
  pulumi_azure: 6.40.0+1

dev_dependencies:
  lints: ^6.1.0
