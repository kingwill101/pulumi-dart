name: automation_inline_local_hybrid_cli
description: Pulumi CLI wrapper for the inline/local hybrid automation example.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  pulumi: ^3.0.0
  automation_inline_local_hybrid_infra:
    path: ../infra

dev_dependencies:
  lints: ^6.1.0
