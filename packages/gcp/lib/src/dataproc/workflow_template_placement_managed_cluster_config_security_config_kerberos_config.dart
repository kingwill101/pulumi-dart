// ignore_for_file: unused_element, unnecessary_cast

class WorkflowTemplatePlacementManagedClusterConfigSecurityConfigKerberosConfig {
  /// The admin server (IP or hostname) for the remote trusted realm in a cross realm trust relationship.
  final String? crossRealmTrustAdminServer;

  /// The KDC (IP or hostname) for the remote trusted realm in a cross realm trust relationship.
  final String? crossRealmTrustKdc;

  /// The remote realm the Dataproc on-cluster KDC will trust, should the user enable cross realm trust.
  final String? crossRealmTrustRealm;

  /// The Cloud Storage URI of a KMS encrypted file containing the shared password between the on-cluster Kerberos realm and the remote trusted realm, in a cross realm trust relationship.
  final String? crossRealmTrustSharedPassword;

  /// Flag to indicate whether to Kerberize the cluster (default: false). Set this field to true to enable Kerberos on a cluster.
  final bool? enableKerberos;

  /// The Cloud Storage URI of a KMS encrypted file containing the master key of the KDC database.
  final String? kdcDbKey;

  /// The Cloud Storage URI of a KMS encrypted file containing the password to the user provided key. For the self-signed certificate, this password is generated by Dataproc.
  final String? keyPassword;

  /// The Cloud Storage URI of the keystore file used for SSL encryption. If not provided, Dataproc will provide a self-signed certificate.
  final String? keystore;

  /// The Cloud Storage URI of a KMS encrypted file containing the password to the user provided keystore. For the self-signed certificate, this password is generated by Dataproc.
  final String? keystorePassword;

  /// The uri of the KMS key used to encrypt various sensitive files.
  final String? kmsKey;

  /// The name of the on-cluster Kerberos realm. If not specified, the uppercased domain of hostnames will be the realm.
  final String? realm;

  /// The Cloud Storage URI of a KMS encrypted file containing the root principal password.
  final String? rootPrincipalPassword;

  /// The lifetime of the ticket granting ticket, in hours. If not specified, or user specifies 0, then default value 10 will be used.
  final int? tgtLifetimeHours;

  /// The Cloud Storage URI of the truststore file used for SSL encryption. If not provided, Dataproc will provide a self-signed certificate.
  final String? truststore;

  /// The Cloud Storage URI of a KMS encrypted file containing the password to the user provided truststore. For the self-signed certificate, this password is generated by Dataproc.
  final String? truststorePassword;

  /// Creates a new [WorkflowTemplatePlacementManagedClusterConfigSecurityConfigKerberosConfig].
  /// [crossRealmTrustAdminServer] The admin server (IP or hostname) for the remote trusted realm in a cross realm trust relationship.
  /// [crossRealmTrustKdc] The KDC (IP or hostname) for the remote trusted realm in a cross realm trust relationship.
  /// [crossRealmTrustRealm] The remote realm the Dataproc on-cluster KDC will trust, should the user enable cross realm trust.
  /// [crossRealmTrustSharedPassword] The Cloud Storage URI of a KMS encrypted file containing the shared password between the on-cluster Kerberos realm and the remote trusted realm, in a cross realm trust relationship.
  /// [enableKerberos] Flag to indicate whether to Kerberize the cluster (default: false). Set this field to true to enable Kerberos on a cluster.
  /// [kdcDbKey] The Cloud Storage URI of a KMS encrypted file containing the master key of the KDC database.
  /// [keyPassword] The Cloud Storage URI of a KMS encrypted file containing the password to the user provided key. For the self-signed certificate, this password is generated by Dataproc.
  /// [keystore] The Cloud Storage URI of the keystore file used for SSL encryption. If not provided, Dataproc will provide a self-signed certificate.
  /// [keystorePassword] The Cloud Storage URI of a KMS encrypted file containing the password to the user provided keystore. For the self-signed certificate, this password is generated by Dataproc.
  /// [kmsKey] The uri of the KMS key used to encrypt various sensitive files.
  /// [realm] The name of the on-cluster Kerberos realm. If not specified, the uppercased domain of hostnames will be the realm.
  /// [rootPrincipalPassword] The Cloud Storage URI of a KMS encrypted file containing the root principal password.
  /// [tgtLifetimeHours] The lifetime of the ticket granting ticket, in hours. If not specified, or user specifies 0, then default value 10 will be used.
  /// [truststore] The Cloud Storage URI of the truststore file used for SSL encryption. If not provided, Dataproc will provide a self-signed certificate.
  /// [truststorePassword] The Cloud Storage URI of a KMS encrypted file containing the password to the user provided truststore. For the self-signed certificate, this password is generated by Dataproc.
  WorkflowTemplatePlacementManagedClusterConfigSecurityConfigKerberosConfig({
    this.crossRealmTrustAdminServer,
    this.crossRealmTrustKdc,
    this.crossRealmTrustRealm,
    this.crossRealmTrustSharedPassword,
    this.enableKerberos,
    this.kdcDbKey,
    this.keyPassword,
    this.keystore,
    this.keystorePassword,
    this.kmsKey,
    this.realm,
    this.rootPrincipalPassword,
    this.tgtLifetimeHours,
    this.truststore,
    this.truststorePassword,
  });

  Map<String, dynamic> toMap() {
    final map = <String, dynamic>{};
    final crossRealmTrustAdminServerValue = crossRealmTrustAdminServer;
    if (crossRealmTrustAdminServerValue != null) {
      map['crossRealmTrustAdminServer'] = crossRealmTrustAdminServerValue;
    }
    final crossRealmTrustKdcValue = crossRealmTrustKdc;
    if (crossRealmTrustKdcValue != null) {
      map['crossRealmTrustKdc'] = crossRealmTrustKdcValue;
    }
    final crossRealmTrustRealmValue = crossRealmTrustRealm;
    if (crossRealmTrustRealmValue != null) {
      map['crossRealmTrustRealm'] = crossRealmTrustRealmValue;
    }
    final crossRealmTrustSharedPasswordValue = crossRealmTrustSharedPassword;
    if (crossRealmTrustSharedPasswordValue != null) {
      map['crossRealmTrustSharedPassword'] = crossRealmTrustSharedPasswordValue;
    }
    final enableKerberosValue = enableKerberos;
    if (enableKerberosValue != null) {
      map['enableKerberos'] = enableKerberosValue;
    }
    final kdcDbKeyValue = kdcDbKey;
    if (kdcDbKeyValue != null) {
      map['kdcDbKey'] = kdcDbKeyValue;
    }
    final keyPasswordValue = keyPassword;
    if (keyPasswordValue != null) {
      map['keyPassword'] = keyPasswordValue;
    }
    final keystoreValue = keystore;
    if (keystoreValue != null) {
      map['keystore'] = keystoreValue;
    }
    final keystorePasswordValue = keystorePassword;
    if (keystorePasswordValue != null) {
      map['keystorePassword'] = keystorePasswordValue;
    }
    final kmsKeyValue = kmsKey;
    if (kmsKeyValue != null) {
      map['kmsKey'] = kmsKeyValue;
    }
    final realmValue = realm;
    if (realmValue != null) {
      map['realm'] = realmValue;
    }
    final rootPrincipalPasswordValue = rootPrincipalPassword;
    if (rootPrincipalPasswordValue != null) {
      map['rootPrincipalPassword'] = rootPrincipalPasswordValue;
    }
    final tgtLifetimeHoursValue = tgtLifetimeHours;
    if (tgtLifetimeHoursValue != null) {
      map['tgtLifetimeHours'] = tgtLifetimeHoursValue;
    }
    final truststoreValue = truststore;
    if (truststoreValue != null) {
      map['truststore'] = truststoreValue;
    }
    final truststorePasswordValue = truststorePassword;
    if (truststorePasswordValue != null) {
      map['truststorePassword'] = truststorePasswordValue;
    }
    return map;
  }

  factory WorkflowTemplatePlacementManagedClusterConfigSecurityConfigKerberosConfig.fromMap(
      Map<String, dynamic> map) {
    return WorkflowTemplatePlacementManagedClusterConfigSecurityConfigKerberosConfig(
      crossRealmTrustAdminServer: map['crossRealmTrustAdminServer'] == null
          ? null
          : map['crossRealmTrustAdminServer'] as String,
      crossRealmTrustKdc: map['crossRealmTrustKdc'] == null
          ? null
          : map['crossRealmTrustKdc'] as String,
      crossRealmTrustRealm: map['crossRealmTrustRealm'] == null
          ? null
          : map['crossRealmTrustRealm'] as String,
      crossRealmTrustSharedPassword:
          map['crossRealmTrustSharedPassword'] == null
              ? null
              : map['crossRealmTrustSharedPassword'] as String,
      enableKerberos:
          map['enableKerberos'] == null ? null : map['enableKerberos'] as bool,
      kdcDbKey: map['kdcDbKey'] == null ? null : map['kdcDbKey'] as String,
      keyPassword:
          map['keyPassword'] == null ? null : map['keyPassword'] as String,
      keystore: map['keystore'] == null ? null : map['keystore'] as String,
      keystorePassword: map['keystorePassword'] == null
          ? null
          : map['keystorePassword'] as String,
      kmsKey: map['kmsKey'] == null ? null : map['kmsKey'] as String,
      realm: map['realm'] == null ? null : map['realm'] as String,
      rootPrincipalPassword: map['rootPrincipalPassword'] == null
          ? null
          : map['rootPrincipalPassword'] as String,
      tgtLifetimeHours: map['tgtLifetimeHours'] == null
          ? null
          : map['tgtLifetimeHours'] as int,
      truststore:
          map['truststore'] == null ? null : map['truststore'] as String,
      truststorePassword: map['truststorePassword'] == null
          ? null
          : map['truststorePassword'] as String,
    );
  }
}
