// ignore_for_file: unused_element, unnecessary_cast

import 'package:pulumi/pulumi.dart' as pulumi;
import '../region_url_map_path_matcher_default_route_action_cors_policy/region_url_map_path_matcher_default_route_action_cors_policy.dart';
import '../region_url_map_path_matcher_default_route_action_fault_injection_policy/region_url_map_path_matcher_default_route_action_fault_injection_policy.dart';
import '../region_url_map_path_matcher_default_route_action_max_stream_duration/region_url_map_path_matcher_default_route_action_max_stream_duration.dart';
import '../region_url_map_path_matcher_default_route_action_request_mirror_policy/region_url_map_path_matcher_default_route_action_request_mirror_policy.dart';
import '../region_url_map_path_matcher_default_route_action_retry_policy/region_url_map_path_matcher_default_route_action_retry_policy.dart';
import '../region_url_map_path_matcher_default_route_action_timeout/region_url_map_path_matcher_default_route_action_timeout.dart';
import '../region_url_map_path_matcher_default_route_action_url_rewrite/region_url_map_path_matcher_default_route_action_url_rewrite.dart';
import '../region_url_map_path_matcher_default_route_action_weighted_backend_service/region_url_map_path_matcher_default_route_action_weighted_backend_service.dart';

class RegionUrlMapPathMatcherDefaultRouteAction {
  /// The specification for allowing client side cross-origin requests. Please see
  /// [W3C Recommendation for Cross Origin Resource Sharing](https://www.w3.org/TR/cors/)
  /// Structure is documented below.
  final RegionUrlMapPathMatcherDefaultRouteActionCorsPolicy? corsPolicy;

  /// The specification for fault injection introduced into traffic to test the resiliency of clients to backend service failure.
  /// As part of fault injection, when clients send requests to a backend service, delays can be introduced by a load balancer on a percentage of requests before sending those requests to the backend service.
  /// Similarly requests from clients can be aborted by the load balancer for a percentage of requests.
  /// timeout and retryPolicy is ignored by clients that are configured with a faultInjectionPolicy if: 1. The traffic is generated by fault injection AND 2. The fault injection is not a delay fault injection.
  /// Fault injection is not supported with the global external HTTP(S) load balancer (classic). To see which load balancers support fault injection, see Load balancing: [Routing and traffic management features](https://cloud.google.com/load-balancing/docs/features#routing-traffic-management).
  /// Structure is documented below.
  final RegionUrlMapPathMatcherDefaultRouteActionFaultInjectionPolicy?
      faultInjectionPolicy;

  /// Specifies the maximum duration (timeout) for streams on the selected route.
  /// Unlike the `Timeout` field where the timeout duration starts from the time the request
  /// has been fully processed (known as end-of-stream), the duration in this field
  /// is computed from the beginning of the stream until the response has been processed,
  /// including all retries. A stream that does not complete in this duration is closed.
  /// Structure is documented below.
  final RegionUrlMapPathMatcherDefaultRouteActionMaxStreamDuration?
      maxStreamDuration;

  /// Specifies the policy on how requests intended for the route's backends are shadowed to a separate mirrored backend service.
  /// The load balancer does not wait for responses from the shadow service. Before sending traffic to the shadow service, the host / authority header is suffixed with -shadow.
  /// Not supported when the URL map is bound to a target gRPC proxy that has the validateForProxyless field set to true.
  /// Structure is documented below.
  final RegionUrlMapPathMatcherDefaultRouteActionRequestMirrorPolicy?
      requestMirrorPolicy;

  /// Specifies the retry policy associated with this route.
  /// Structure is documented below.
  final RegionUrlMapPathMatcherDefaultRouteActionRetryPolicy? retryPolicy;

  /// Specifies the timeout for the selected route. Timeout is computed from the time the request has been fully processed (known as end-of-stream) up until the response has been processed. Timeout includes all retries.
  /// If not specified, this field uses the largest timeout among all backend services associated with the route.
  /// Not supported when the URL map is bound to a target gRPC proxy that has validateForProxyless field set to true.
  /// Structure is documented below.
  final RegionUrlMapPathMatcherDefaultRouteActionTimeout? timeout;

  /// The spec to modify the URL of the request, before forwarding the request to the matched service.
  /// urlRewrite is the only action supported in UrlMaps for external HTTP(S) load balancers.
  /// Not supported when the URL map is bound to a target gRPC proxy that has the validateForProxyless field set to true.
  /// Structure is documented below.
  final RegionUrlMapPathMatcherDefaultRouteActionUrlRewrite? urlRewrite;

  /// A list of weighted backend services to send traffic to when a route match occurs. The weights determine the fraction of traffic that flows to their corresponding backend service. If all traffic needs to go to a single backend service, there must be one weightedBackendService with weight set to a non-zero number.
  /// After a backend service is identified and before forwarding the request to the backend service, advanced routing actions such as URL rewrites and header transformations are applied depending on additional settings specified in this HttpRouteAction.
  /// Structure is documented below.
  final List<RegionUrlMapPathMatcherDefaultRouteActionWeightedBackendService>?
      weightedBackendServices;

  RegionUrlMapPathMatcherDefaultRouteAction({
    this.corsPolicy,
    this.faultInjectionPolicy,
    this.maxStreamDuration,
    this.requestMirrorPolicy,
    this.retryPolicy,
    this.timeout,
    this.urlRewrite,
    this.weightedBackendServices,
  });

  Map<String, dynamic> toMap() {
    final map = <String, dynamic>{};
    final corsPolicyValue = corsPolicy;
    if (corsPolicyValue != null) {
      map['corsPolicy'] = corsPolicyValue.toMap();
    }
    final faultInjectionPolicyValue = faultInjectionPolicy;
    if (faultInjectionPolicyValue != null) {
      map['faultInjectionPolicy'] = faultInjectionPolicyValue.toMap();
    }
    final maxStreamDurationValue = maxStreamDuration;
    if (maxStreamDurationValue != null) {
      map['maxStreamDuration'] = maxStreamDurationValue.toMap();
    }
    final requestMirrorPolicyValue = requestMirrorPolicy;
    if (requestMirrorPolicyValue != null) {
      map['requestMirrorPolicy'] = requestMirrorPolicyValue.toMap();
    }
    final retryPolicyValue = retryPolicy;
    if (retryPolicyValue != null) {
      map['retryPolicy'] = retryPolicyValue.toMap();
    }
    final timeoutValue = timeout;
    if (timeoutValue != null) {
      map['timeout'] = timeoutValue.toMap();
    }
    final urlRewriteValue = urlRewrite;
    if (urlRewriteValue != null) {
      map['urlRewrite'] = urlRewriteValue.toMap();
    }
    final weightedBackendServicesValue = weightedBackendServices;
    if (weightedBackendServicesValue != null) {
      map['weightedBackendServices'] = pulumi.Input.encodeList<
              RegionUrlMapPathMatcherDefaultRouteActionWeightedBackendService,
              Map<String, dynamic>>(
          weightedBackendServicesValue, (value) => value.toMap());
    }
    return map;
  }

  factory RegionUrlMapPathMatcherDefaultRouteAction.fromMap(
      Map<String, dynamic> map) {
    return RegionUrlMapPathMatcherDefaultRouteAction(
      corsPolicy: map['corsPolicy'] == null
          ? null
          : RegionUrlMapPathMatcherDefaultRouteActionCorsPolicy.fromMap(
              (map['corsPolicy'] as Map).cast<String, dynamic>()),
      faultInjectionPolicy: map['faultInjectionPolicy'] == null
          ? null
          : RegionUrlMapPathMatcherDefaultRouteActionFaultInjectionPolicy
              .fromMap(
                  (map['faultInjectionPolicy'] as Map).cast<String, dynamic>()),
      maxStreamDuration: map['maxStreamDuration'] == null
          ? null
          : RegionUrlMapPathMatcherDefaultRouteActionMaxStreamDuration.fromMap(
              (map['maxStreamDuration'] as Map).cast<String, dynamic>()),
      requestMirrorPolicy: map['requestMirrorPolicy'] == null
          ? null
          : RegionUrlMapPathMatcherDefaultRouteActionRequestMirrorPolicy
              .fromMap(
                  (map['requestMirrorPolicy'] as Map).cast<String, dynamic>()),
      retryPolicy: map['retryPolicy'] == null
          ? null
          : RegionUrlMapPathMatcherDefaultRouteActionRetryPolicy.fromMap(
              (map['retryPolicy'] as Map).cast<String, dynamic>()),
      timeout: map['timeout'] == null
          ? null
          : RegionUrlMapPathMatcherDefaultRouteActionTimeout.fromMap(
              (map['timeout'] as Map).cast<String, dynamic>()),
      urlRewrite: map['urlRewrite'] == null
          ? null
          : RegionUrlMapPathMatcherDefaultRouteActionUrlRewrite.fromMap(
              (map['urlRewrite'] as Map).cast<String, dynamic>()),
      weightedBackendServices: map['weightedBackendServices'] == null
          ? null
          : pulumi.Input.decodeList<
                  RegionUrlMapPathMatcherDefaultRouteActionWeightedBackendService>(
              map['weightedBackendServices'],
              (value) =>
                  RegionUrlMapPathMatcherDefaultRouteActionWeightedBackendService
                      .fromMap((value as Map).cast<String, dynamic>())),
    );
  }
}
