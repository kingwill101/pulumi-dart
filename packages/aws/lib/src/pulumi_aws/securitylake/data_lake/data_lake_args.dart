// ignore_for_file: unused_element, unnecessary_cast

import 'package:pulumi/pulumi.dart';
import '../data_lake_configuration/data_lake_configuration.dart';
import '../data_lake_timeouts/data_lake_timeouts.dart';

/// The set of arguments for DataLake.
class DataLakeArgs {
  /// Specify the Region or Regions that will contribute data to the rollup region.
  final Input<DataLakeConfiguration> configuration;

  /// The Amazon Resource Name (ARN) used to create and update the AWS Glue table. This table contains partitions generated by the ingestion and normalization of AWS log sources and custom sources.
  final Input<String> metaStoreManagerRoleArn;

  /// The AWS Regions where Security Lake is automatically enabled.
  final Input<String>? region;

  /// Key-value map of resource tags. If configured with a provider `default_tags` configuration block present, tags with matching keys will overwrite those defined at the provider-level.
  final Input<Map<String, String>>? tags;
  final Input<DataLakeTimeouts>? timeouts;

  DataLakeArgs({
    required this.configuration,
    required this.metaStoreManagerRoleArn,
    this.region,
    this.tags,
    this.timeouts,
  });

  Map<String, dynamic> toMap() {
    final map = <String, dynamic>{};
    map['configuration'] =
        Input.mapInputValue<DataLakeConfiguration, Map<String, dynamic>>(
            configuration, (value) => value.toMap());
    map['metaStoreManagerRoleArn'] = metaStoreManagerRoleArn;
    final regionValue = region;
    if (regionValue != null) {
      map['region'] = regionValue;
    }
    final tagsValue = tags;
    if (tagsValue != null) {
      map['tags'] = tagsValue;
    }
    final timeoutsValue = timeouts;
    if (timeoutsValue != null) {
      map['timeouts'] =
          Input.mapOptionalInputValue<DataLakeTimeouts, Map<String, dynamic>>(
              timeoutsValue, (value) => value.toMap());
    }
    return map;
  }

  factory DataLakeArgs.fromMap(Map<String, dynamic> map) {
    return DataLakeArgs(
      configuration: Input.asInput<DataLakeConfiguration>(map['configuration']),
      metaStoreManagerRoleArn:
          Input.asInput<String>(map['metaStoreManagerRoleArn']),
      region: Input.asOptionalInput<String>(map['region']),
      tags: Input.asOptionalInput<Map<String, String>>(map['tags']),
      timeouts: Input.asOptionalInput<DataLakeTimeouts>(map['timeouts']),
    );
  }
}
