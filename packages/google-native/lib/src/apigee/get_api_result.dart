// ignore_for_file: unused_element, unnecessary_cast

import 'google_cloud_apigee_v1_entity_metadata_response.dart';

/// Result data returned by getApi.
class GetApiResult {
  /// The type of the API proxy.
  final String apiProxyType;
  /// User labels applied to this API Proxy.
  final Map<String, String> labels;
  /// The id of the most recently created revision for this api proxy.
  final String latestRevisionId;
  /// Metadata describing the API proxy.
  final GoogleCloudApigeeV1EntityMetadataResponse metaData;
  /// Name of the API proxy.
  final String name;
  /// Whether this proxy is read-only. A read-only proxy cannot have new revisions created through calls to CreateApiProxyRevision. A proxy is read-only if it was generated by an archive.
  final bool readOnly;
  /// List of revisions defined for the API proxy.
  final List<String> revision;

  /// Creates a new [GetApiResult].
  /// [apiProxyType] The type of the API proxy.
  /// [labels] User labels applied to this API Proxy.
  /// [latestRevisionId] The id of the most recently created revision for this api proxy.
  /// [metaData] Metadata describing the API proxy.
  /// [name] Name of the API proxy.
  /// [readOnly] Whether this proxy is read-only. A read-only proxy cannot have new revisions created through calls to CreateApiProxyRevision. A proxy is read-only if it was generated by an archive.
  /// [revision] List of revisions defined for the API proxy.
  GetApiResult({
    required this.apiProxyType,
    required this.labels,
    required this.latestRevisionId,
    required this.metaData,
    required this.name,
    required this.readOnly,
    required this.revision,
  });

  Map<String, dynamic> toMap() {
    return <String, dynamic>{
      'apiProxyType': apiProxyType,
      'labels': labels,
      'latestRevisionId': latestRevisionId,
      'metaData': metaData.toMap(),
      'name': name,
      'readOnly': readOnly,
      'revision': revision,
    };
  }

  factory GetApiResult.fromMap(Map<String, dynamic> map) {
    return GetApiResult(
      apiProxyType: map['apiProxyType'] as String,
      labels: (map['labels'] as Map).cast<String, String>(),
      latestRevisionId: map['latestRevisionId'] as String,
      metaData: GoogleCloudApigeeV1EntityMetadataResponse.fromMap((map['metaData'] as Map).cast<String, dynamic>()),
      name: map['name'] as String,
      readOnly: map['readOnly'] as bool,
      revision: (map['revision'] as List).cast<String>(),
    );
  }
}

