// ignore_for_file: unused_element, unnecessary_cast

import 'package:pulumi/pulumi.dart' hide Config;
import 'allocated_connection_response2.dart';

/// Result data returned by getAppGateway.
class GetAppGatewayResult2 {
  /// A list of connections allocated for the Gateway
  final List<AllocatedConnectionResponse2> allocatedConnections;

  /// Timestamp when the resource was created.
  final String createTime;

  /// Optional. An arbitrary user-provided name for the AppGateway. Cannot exceed 64 characters.
  final String displayName;

  /// The type of hosting used by the AppGateway.
  final String hostType;

  /// Optional. Resource labels to represent user provided metadata.
  final Map<String, String> labels;

  /// Unique resource name of the AppGateway. The name is ignored when creating an AppGateway.
  final String name;

  /// The current state of the AppGateway.
  final String state;

  /// The type of network connectivity used by the AppGateway.
  final String type;

  /// A unique identifier for the instance generated by the system.
  final String uid;

  /// Timestamp when the resource was last modified.
  final String updateTime;

  /// Server-defined URI for this resource.
  final String uri;

  GetAppGatewayResult2({
    required this.allocatedConnections,
    required this.createTime,
    required this.displayName,
    required this.hostType,
    required this.labels,
    required this.name,
    required this.state,
    required this.type,
    required this.uid,
    required this.updateTime,
    required this.uri,
  });

  Map<String, dynamic> toMap() {
    final map = <String, dynamic>{};
    map['allocatedConnections'] =
        Input.encodeList<AllocatedConnectionResponse2, Map<String, dynamic>>(
            allocatedConnections, (value) => value.toMap());
    map['createTime'] = createTime;
    map['displayName'] = displayName;
    map['hostType'] = hostType;
    map['labels'] = labels;
    map['name'] = name;
    map['state'] = state;
    map['type'] = type;
    map['uid'] = uid;
    map['updateTime'] = updateTime;
    map['uri'] = uri;
    return map;
  }

  factory GetAppGatewayResult2.fromMap(Map<String, dynamic> map) {
    return GetAppGatewayResult2(
      allocatedConnections: Input.decodeList<AllocatedConnectionResponse2>(
          map['allocatedConnections'],
          (value) => AllocatedConnectionResponse2.fromMap(
              (value as Map).cast<String, dynamic>())),
      createTime: map['createTime'] as String,
      displayName: map['displayName'] as String,
      hostType: map['hostType'] as String,
      labels: (map['labels'] as Map).cast<String, String>(),
      name: map['name'] as String,
      state: map['state'] as String,
      type: map['type'] as String,
      uid: map['uid'] as String,
      updateTime: map['updateTime'] as String,
      uri: map['uri'] as String,
    );
  }
}
