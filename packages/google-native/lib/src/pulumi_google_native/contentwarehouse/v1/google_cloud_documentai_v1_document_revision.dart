// ignore_for_file: unused_element, unnecessary_cast

import 'google_cloud_documentai_v1_document_revision_human_review.dart';

/// Contains past or forward revisions of this document.
class GoogleCloudDocumentaiV1DocumentRevision {
  /// If the change was made by a person specify the name or id of that person.
  final String? agent;

  /// The time that the revision was created, internally generated by doc proto storage at the time of create.
  final String? createTime;

  /// Human Review information of this revision.
  final GoogleCloudDocumentaiV1DocumentRevisionHumanReview? humanReview;

  /// Id of the revision, internally generated by doc proto storage. Unique within the context of the document.
  final String? id;

  /// The revisions that this revision is based on. This can include one or more parent (when documents are merged.) This field represents the index into the `revisions` field.
  final List<int>? parent;

  /// The revisions that this revision is based on. Must include all the ids that have anything to do with this revision - eg. there are `provenance.parent.revision` fields that index into this field.
  final List<String>? parentIds;

  /// If the annotation was made by processor identify the processor by its resource name.
  final String? processor;

  GoogleCloudDocumentaiV1DocumentRevision({
    this.agent,
    this.createTime,
    this.humanReview,
    this.id,
    this.parent,
    this.parentIds,
    this.processor,
  });

  Map<String, dynamic> toMap() {
    final map = <String, dynamic>{};
    final agentValue = agent;
    if (agentValue != null) {
      map['agent'] = agentValue;
    }
    final createTimeValue = createTime;
    if (createTimeValue != null) {
      map['createTime'] = createTimeValue;
    }
    final humanReviewValue = humanReview;
    if (humanReviewValue != null) {
      map['humanReview'] = humanReviewValue.toMap();
    }
    final idValue = id;
    if (idValue != null) {
      map['id'] = idValue;
    }
    final parentValue = parent;
    if (parentValue != null) {
      map['parent'] = parentValue;
    }
    final parentIdsValue = parentIds;
    if (parentIdsValue != null) {
      map['parentIds'] = parentIdsValue;
    }
    final processorValue = processor;
    if (processorValue != null) {
      map['processor'] = processorValue;
    }
    return map;
  }

  factory GoogleCloudDocumentaiV1DocumentRevision.fromMap(
      Map<String, dynamic> map) {
    return GoogleCloudDocumentaiV1DocumentRevision(
      agent: map['agent'] == null ? null : map['agent'] as String,
      createTime:
          map['createTime'] == null ? null : map['createTime'] as String,
      humanReview: map['humanReview'] == null
          ? null
          : GoogleCloudDocumentaiV1DocumentRevisionHumanReview.fromMap(
              (map['humanReview'] as Map).cast<String, dynamic>()),
      id: map['id'] == null ? null : map['id'] as String,
      parent:
          map['parent'] == null ? null : (map['parent'] as List).cast<int>(),
      parentIds: map['parentIds'] == null
          ? null
          : (map['parentIds'] as List).cast<String>(),
      processor: map['processor'] == null ? null : map['processor'] as String,
    );
  }
}
