// ignore_for_file: unused_element, unnecessary_cast

/// Options to configure rule type SingleColumnChange. The rule is used to change the properties of a column. The rule filter field can refer to one entity. The rule scope can be one of: Column. When using this rule, if a field is not specified than the destination column's configuration will be the same as the one in the source column..
class SingleColumnChangeResponse {
  /// Optional. Is the column of array type.
  final bool array;

  /// Optional. The length of the array, only relevant if the column type is an array.
  final int arrayLength;

  /// Optional. Is the column auto-generated/identity.
  final bool autoGenerated;

  /// Optional. Charset override - instead of table level charset.
  final String charset;

  /// Optional. Collation override - instead of table level collation.
  final String collation;

  /// Optional. Comment associated with the column.
  final String comment;

  /// Optional. Custom engine specific features.
  final Map<String, String> customFeatures;

  /// Optional. Column data type name.
  final String dataType;

  /// Optional. Column fractional seconds precision - e.g. 2 as in timestamp (2) - when relevant.
  final int fractionalSecondsPrecision;

  /// Optional. Column length - e.g. 50 as in varchar (50) - when relevant.
  final String length;

  /// Optional. Is the column nullable.
  final bool nullable;

  /// Optional. Column precision - e.g. 8 as in double (8,2) - when relevant.
  final int precision;

  /// Optional. Column scale - e.g. 2 as in double (8,2) - when relevant.
  final int scale;

  /// Optional. Specifies the list of values allowed in the column.
  final List<String> setValues;

  /// Optional. Is the column a UDT (User-defined Type).
  final bool udt;

  SingleColumnChangeResponse({
    required this.array,
    required this.arrayLength,
    required this.autoGenerated,
    required this.charset,
    required this.collation,
    required this.comment,
    required this.customFeatures,
    required this.dataType,
    required this.fractionalSecondsPrecision,
    required this.length,
    required this.nullable,
    required this.precision,
    required this.scale,
    required this.setValues,
    required this.udt,
  });

  Map<String, dynamic> toMap() {
    final map = <String, dynamic>{};
    map['array'] = array;
    map['arrayLength'] = arrayLength;
    map['autoGenerated'] = autoGenerated;
    map['charset'] = charset;
    map['collation'] = collation;
    map['comment'] = comment;
    map['customFeatures'] = customFeatures;
    map['dataType'] = dataType;
    map['fractionalSecondsPrecision'] = fractionalSecondsPrecision;
    map['length'] = length;
    map['nullable'] = nullable;
    map['precision'] = precision;
    map['scale'] = scale;
    map['setValues'] = setValues;
    map['udt'] = udt;
    return map;
  }

  factory SingleColumnChangeResponse.fromMap(Map<String, dynamic> map) {
    return SingleColumnChangeResponse(
      array: map['array'] as bool,
      arrayLength: map['arrayLength'] as int,
      autoGenerated: map['autoGenerated'] as bool,
      charset: map['charset'] as String,
      collation: map['collation'] as String,
      comment: map['comment'] as String,
      customFeatures: (map['customFeatures'] as Map).cast<String, String>(),
      dataType: map['dataType'] as String,
      fractionalSecondsPrecision: map['fractionalSecondsPrecision'] as int,
      length: map['length'] as String,
      nullable: map['nullable'] as bool,
      precision: map['precision'] as int,
      scale: map['scale'] as int,
      setValues: (map['setValues'] as List).cast<String>(),
      udt: map['udt'] as bool,
    );
  }
}
