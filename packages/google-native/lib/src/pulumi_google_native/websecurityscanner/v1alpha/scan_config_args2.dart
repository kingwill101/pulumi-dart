// ignore_for_file: unused_element, unnecessary_cast

import 'package:pulumi/pulumi.dart' hide Config;
import 'authentication3.dart';
import 'scan_config_target_platforms_item.dart';
import 'scan_config_user_agent2.dart';
import 'scan_run.dart';
import 'schedule4.dart';

/// The set of arguments for ScanConfig.
class ScanConfigArgs2 {
  /// The authentication configuration. If specified, service will use the authentication configuration during scanning.
  final Input<Authentication3>? authentication;

  /// The excluded URL patterns as described in https://cloud.google.com/security-command-center/docs/how-to-use-web-security-scanner#excluding_urls
  final Input<List<String>>? blacklistPatterns;

  /// The user provided display name of the ScanConfig.
  final Input<String> displayName;

  /// Latest ScanRun if available.
  final Input<ScanRun>? latestRun;

  /// The maximum QPS during scanning. A valid value ranges from 5 to 20 inclusively. If the field is unspecified or its value is set 0, server will default to 15. Other values outside of [5, 20] range will be rejected with INVALID_ARGUMENT error.
  final Input<int>? maxQps;

  /// The resource name of the ScanConfig. The name follows the format of 'projects/{projectId}/scanConfigs/{scanConfigId}'. The ScanConfig IDs are generated by the system.
  final Input<String>? name;
  final Input<String>? project;

  /// The schedule of the ScanConfig.
  final Input<Schedule4>? schedule;

  /// The starting URLs from which the scanner finds site pages.
  final Input<List<String>> startingUrls;

  /// Set of Google Cloud platforms targeted by the scan. If empty, APP_ENGINE will be used as a default.
  final Input<List<ScanConfigTargetPlatformsItem>>? targetPlatforms;

  /// The user agent used during scanning.
  final Input<ScanConfigUserAgent2>? userAgent;

  ScanConfigArgs2({
    this.authentication,
    this.blacklistPatterns,
    required this.displayName,
    this.latestRun,
    this.maxQps,
    this.name,
    this.project,
    this.schedule,
    required this.startingUrls,
    this.targetPlatforms,
    this.userAgent,
  });

  Map<String, dynamic> toMap() {
    final map = <String, dynamic>{};
    final authenticationValue = authentication;
    if (authenticationValue != null) {
      map['authentication'] =
          Input.mapOptionalInputValue<Authentication3, Map<String, dynamic>>(
              authenticationValue, (value) => value.toMap());
    }
    final blacklistPatternsValue = blacklistPatterns;
    if (blacklistPatternsValue != null) {
      map['blacklistPatterns'] = blacklistPatternsValue;
    }
    map['displayName'] = displayName;
    final latestRunValue = latestRun;
    if (latestRunValue != null) {
      map['latestRun'] =
          Input.mapOptionalInputValue<ScanRun, Map<String, dynamic>>(
              latestRunValue, (value) => value.toMap());
    }
    final maxQpsValue = maxQps;
    if (maxQpsValue != null) {
      map['maxQps'] = maxQpsValue;
    }
    final nameValue = name;
    if (nameValue != null) {
      map['name'] = nameValue;
    }
    final projectValue = project;
    if (projectValue != null) {
      map['project'] = projectValue;
    }
    final scheduleValue = schedule;
    if (scheduleValue != null) {
      map['schedule'] =
          Input.mapOptionalInputValue<Schedule4, Map<String, dynamic>>(
              scheduleValue, (value) => value.toMap());
    }
    map['startingUrls'] = startingUrls;
    final targetPlatformsValue = targetPlatforms;
    if (targetPlatformsValue != null) {
      map['targetPlatforms'] = Input.mapOptionalInputValue<
              List<ScanConfigTargetPlatformsItem>, List<String>>(
          targetPlatformsValue,
          (value) => Input.encodeList<ScanConfigTargetPlatformsItem, String>(
              value, (value) => value.value));
    }
    final userAgentValue = userAgent;
    if (userAgentValue != null) {
      map['userAgent'] =
          Input.mapOptionalInputValue<ScanConfigUserAgent2, String>(
              userAgentValue, (value) => value.value);
    }
    return map;
  }

  factory ScanConfigArgs2.fromMap(Map<String, dynamic> map) {
    return ScanConfigArgs2(
      authentication:
          Input.asOptionalInput<Authentication3>(map['authentication']),
      blacklistPatterns:
          Input.asOptionalInput<List<String>>(map['blacklistPatterns']),
      displayName: Input.asInput<String>(map['displayName']),
      latestRun: Input.asOptionalInput<ScanRun>(map['latestRun']),
      maxQps: Input.asOptionalInput<int>(map['maxQps']),
      name: Input.asOptionalInput<String>(map['name']),
      project: Input.asOptionalInput<String>(map['project']),
      schedule: Input.asOptionalInput<Schedule4>(map['schedule']),
      startingUrls: Input.asInput<List<String>>(map['startingUrls']),
      targetPlatforms:
          Input.asOptionalInput<List<ScanConfigTargetPlatformsItem>>(
              map['targetPlatforms']),
      userAgent: Input.asOptionalInput<ScanConfigUserAgent2>(map['userAgent']),
    );
  }
}
