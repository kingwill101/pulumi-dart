// ignore_for_file: unused_element, unnecessary_cast

import 'authentication_response3.dart';
import 'scan_run_response.dart';
import 'schedule_response4.dart';

/// Result data returned by getScanConfig.
class GetScanConfigResult2 {
  /// The authentication configuration. If specified, service will use the authentication configuration during scanning.
  final AuthenticationResponse3 authentication;

  /// The excluded URL patterns as described in https://cloud.google.com/security-command-center/docs/how-to-use-web-security-scanner#excluding_urls
  final List<String> blacklistPatterns;

  /// The user provided display name of the ScanConfig.
  final String displayName;

  /// Latest ScanRun if available.
  final ScanRunResponse latestRun;

  /// The maximum QPS during scanning. A valid value ranges from 5 to 20 inclusively. If the field is unspecified or its value is set 0, server will default to 15. Other values outside of [5, 20] range will be rejected with INVALID_ARGUMENT error.
  final int maxQps;

  /// The resource name of the ScanConfig. The name follows the format of 'projects/{projectId}/scanConfigs/{scanConfigId}'. The ScanConfig IDs are generated by the system.
  final String name;

  /// The schedule of the ScanConfig.
  final ScheduleResponse4 schedule;

  /// The starting URLs from which the scanner finds site pages.
  final List<String> startingUrls;

  /// Set of Google Cloud platforms targeted by the scan. If empty, APP_ENGINE will be used as a default.
  final List<String> targetPlatforms;

  /// The user agent used during scanning.
  final String userAgent;

  GetScanConfigResult2({
    required this.authentication,
    required this.blacklistPatterns,
    required this.displayName,
    required this.latestRun,
    required this.maxQps,
    required this.name,
    required this.schedule,
    required this.startingUrls,
    required this.targetPlatforms,
    required this.userAgent,
  });

  Map<String, dynamic> toMap() {
    final map = <String, dynamic>{};
    map['authentication'] = authentication.toMap();
    map['blacklistPatterns'] = blacklistPatterns;
    map['displayName'] = displayName;
    map['latestRun'] = latestRun.toMap();
    map['maxQps'] = maxQps;
    map['name'] = name;
    map['schedule'] = schedule.toMap();
    map['startingUrls'] = startingUrls;
    map['targetPlatforms'] = targetPlatforms;
    map['userAgent'] = userAgent;
    return map;
  }

  factory GetScanConfigResult2.fromMap(Map<String, dynamic> map) {
    return GetScanConfigResult2(
      authentication: AuthenticationResponse3.fromMap(
          (map['authentication'] as Map).cast<String, dynamic>()),
      blacklistPatterns: (map['blacklistPatterns'] as List).cast<String>(),
      displayName: map['displayName'] as String,
      latestRun: ScanRunResponse.fromMap(
          (map['latestRun'] as Map).cast<String, dynamic>()),
      maxQps: map['maxQps'] as int,
      name: map['name'] as String,
      schedule: ScheduleResponse4.fromMap(
          (map['schedule'] as Map).cast<String, dynamic>()),
      startingUrls: (map['startingUrls'] as List).cast<String>(),
      targetPlatforms: (map['targetPlatforms'] as List).cast<String>(),
      userAgent: map['userAgent'] as String,
    );
  }
}
