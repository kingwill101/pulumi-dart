// ignore_for_file: unused_element, unnecessary_cast

import 'package:pulumi/pulumi.dart' hide Config;
import 'deployment_label_entry_response3.dart';
import 'deployment_update_response3.dart';
import 'operation_response4.dart';
import 'target_configuration_response3.dart';

/// Result data returned by getDeployment.
class GetDeploymentResult4 {
  /// An optional user-provided description of the deployment.
  final String description;

  /// Provides a fingerprint to use in requests to modify a deployment, such as `update()`, `stop()`, and `cancelPreview()` requests. A fingerprint is a randomly generated value that must be provided with `update()`, `stop()`, and `cancelPreview()` requests to perform optimistic locking. This ensures optimistic concurrency so that only one request happens at a time. The fingerprint is initially generated by Deployment Manager and changes after every request to modify data. To get the latest fingerprint value, perform a `get()` request to a deployment.
  final String fingerprint;

  /// Creation timestamp in RFC3339 text format.
  final String insertTime;

  /// Map of One Platform labels; provided by the client when the resource is created or updated. Specifically: Label keys must be between 1 and 63 characters long and must conform to the following regular expression: `[a-z]([-a-z0-9]*[a-z0-9])?` Label values must be between 0 and 63 characters long and must conform to the regular expression `([a-z]([-a-z0-9]*[a-z0-9])?)?`.
  final List<DeploymentLabelEntryResponse3> labels;

  /// URL of the manifest representing the last manifest that was successfully deployed. If no manifest has been successfully deployed, this field will be absent.
  final String manifest;

  /// Name of the resource; provided by the client when the resource is created. The name must be 1-63 characters long, and comply with RFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash.
  final String name;

  /// The Operation that most recently ran, or is currently running, on this deployment.
  final OperationResponse4 operation;

  /// Server defined URL for the resource.
  final String selfLink;

  /// [Input Only] The parameters that define your deployment, including the deployment configuration and relevant templates.
  final TargetConfigurationResponse3 target;

  /// If Deployment Manager is currently updating or previewing an update to this deployment, the updated configuration appears here.
  final DeploymentUpdateResponse3 update;

  /// Update timestamp in RFC3339 text format.
  final String updateTime;

  GetDeploymentResult4({
    required this.description,
    required this.fingerprint,
    required this.insertTime,
    required this.labels,
    required this.manifest,
    required this.name,
    required this.operation,
    required this.selfLink,
    required this.target,
    required this.update,
    required this.updateTime,
  });

  Map<String, dynamic> toMap() {
    final map = <String, dynamic>{};
    map['description'] = description;
    map['fingerprint'] = fingerprint;
    map['insertTime'] = insertTime;
    map['labels'] =
        Input.encodeList<DeploymentLabelEntryResponse3, Map<String, dynamic>>(
            labels, (value) => value.toMap());
    map['manifest'] = manifest;
    map['name'] = name;
    map['operation'] = operation.toMap();
    map['selfLink'] = selfLink;
    map['target'] = target.toMap();
    map['update'] = update.toMap();
    map['updateTime'] = updateTime;
    return map;
  }

  factory GetDeploymentResult4.fromMap(Map<String, dynamic> map) {
    return GetDeploymentResult4(
      description: map['description'] as String,
      fingerprint: map['fingerprint'] as String,
      insertTime: map['insertTime'] as String,
      labels: Input.decodeList<DeploymentLabelEntryResponse3>(
          map['labels'],
          (value) => DeploymentLabelEntryResponse3.fromMap(
              (value as Map).cast<String, dynamic>())),
      manifest: map['manifest'] as String,
      name: map['name'] as String,
      operation: OperationResponse4.fromMap(
          (map['operation'] as Map).cast<String, dynamic>()),
      selfLink: map['selfLink'] as String,
      target: TargetConfigurationResponse3.fromMap(
          (map['target'] as Map).cast<String, dynamic>()),
      update: DeploymentUpdateResponse3.fromMap(
          (map['update'] as Map).cast<String, dynamic>()),
      updateTime: map['updateTime'] as String,
    );
  }
}
