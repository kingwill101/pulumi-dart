name: gcp_faas_binary_upload
description: Cloud Run binary upload example for pulumi_dart_faas.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  pulumi: 3.0.0
  pulumi_dart_faas: 0.1.0

dev_dependencies:
  lints: ^6.1.0
