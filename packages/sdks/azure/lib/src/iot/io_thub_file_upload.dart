// ignore_for_file: unused_element, unnecessary_cast

import 'package:pulumi/pulumi.dart' as pulumi;

class IoTHubFileUpload {
  /// The type used to authenticate against the storage account. Possible values are `keyBased` and `identityBased`. Defaults to `keyBased`.
  final pulumi.Input<String?>? authenticationType;
  /// The connection string for the Azure Storage account to which files are uploaded.
  final pulumi.Input<String> connectionString;
  /// The name of the root container where the files should be uploaded to. The container need not exist but should be creatable using the connectionString specified.
  final pulumi.Input<String> containerName;
  /// The period of time for which a file upload notification message is available to consume before it expires, specified as an [ISO 8601 timespan duration](https://en.wikipedia.org/wiki/ISO_8601#Durations). This value must be between 1 minute and 48 hours. Defaults to `PT1H`.
  final pulumi.Input<String?>? defaultTtl;
  /// The ID of the User Managed Identity used to authenticate against the storage account.
  ///
  /// &gt; **Note:** `identityId` can only be specified when `authenticationType` is `identityBased`. It must be one of the `identityIds` of the IoT Hub. If `identityId` is omitted when `authenticationType` is `identityBased`, then the System-Assigned Managed Identity of the IoT Hub will be used.
  ///
  /// &gt; **Note:** An IoT Hub can only be updated to use the System-Assigned Managed Identity for `fileUpload` since it is not possible to grant access to the endpoint until after creation.
  final pulumi.Input<String?>? identityId;
  /// The lock duration for the file upload notifications queue, specified as an [ISO 8601 timespan duration](https://en.wikipedia.org/wiki/ISO_8601#Durations). This value must be between 5 and 300 seconds. Defaults to `PT1M`.
  final pulumi.Input<String?>? lockDuration;
  /// The number of times the IoT Hub attempts to deliver a file upload notification message. Defaults to `10`.
  final pulumi.Input<int?>? maxDeliveryCount;
  /// Used to specify whether file notifications are sent to IoT Hub on upload. Defaults to `false`.
  final pulumi.Input<bool?>? notifications;
  /// The period of time for which the SAS URI generated by IoT Hub for file upload is valid, specified as an [ISO 8601 timespan duration](https://en.wikipedia.org/wiki/ISO_8601#Durations). This value must be between 1 minute and 24 hours. Defaults to `PT1H`.
  final pulumi.Input<String?>? sasTtl;

  /// Creates a new [IoTHubFileUpload].
  /// [authenticationType] The type used to authenticate against the storage account. Possible values are `keyBased` and `identityBased`. Defaults to `keyBased`.
  /// [connectionString] The connection string for the Azure Storage account to which files are uploaded.
  /// [containerName] The name of the root container where the files should be uploaded to. The container need not exist but should be creatable using the connectionString specified.
  /// [defaultTtl] The period of time for which a file upload notification message is available to consume before it expires, specified as an [ISO 8601 timespan duration](https://en.wikipedia.org/wiki/ISO_8601#Durations). This value must be between 1 minute and 48 hours. Defaults to `PT1H`.
  /// [identityId] The ID of the User Managed Identity used to authenticate against the storage account.
  /// [lockDuration] The lock duration for the file upload notifications queue, specified as an [ISO 8601 timespan duration](https://en.wikipedia.org/wiki/ISO_8601#Durations). This value must be between 5 and 300 seconds. Defaults to `PT1M`.
  /// [maxDeliveryCount] The number of times the IoT Hub attempts to deliver a file upload notification message. Defaults to `10`.
  /// [notifications] Used to specify whether file notifications are sent to IoT Hub on upload. Defaults to `false`.
  /// [sasTtl] The period of time for which the SAS URI generated by IoT Hub for file upload is valid, specified as an [ISO 8601 timespan duration](https://en.wikipedia.org/wiki/ISO_8601#Durations). This value must be between 1 minute and 24 hours. Defaults to `PT1H`.
  const IoTHubFileUpload({
    this.authenticationType,
    required this.connectionString,
    required this.containerName,
    this.defaultTtl,
    this.identityId,
    this.lockDuration,
    this.maxDeliveryCount,
    this.notifications,
    this.sasTtl,
  });

  Map<String, dynamic> toMap() {
    return <String, dynamic>{
      'authenticationType': ?authenticationType,
      'connectionString': connectionString,
      'containerName': containerName,
      'defaultTtl': ?defaultTtl,
      'identityId': ?identityId,
      'lockDuration': ?lockDuration,
      'maxDeliveryCount': ?maxDeliveryCount,
      'notifications': ?notifications,
      'sasTtl': ?sasTtl,
    };
  }

  factory IoTHubFileUpload.fromMap(Map<String, dynamic> map) {
    return IoTHubFileUpload(
      authenticationType: (() { final guardedValue = map['authenticationType']; if (guardedValue == null) return null; return pulumi.Input.fromValue(guardedValue as String); })(),
      connectionString: pulumi.Input.fromValue(map['connectionString'] as String),
      containerName: pulumi.Input.fromValue(map['containerName'] as String),
      defaultTtl: (() { final guardedValue = map['defaultTtl']; if (guardedValue == null) return null; return pulumi.Input.fromValue(guardedValue as String); })(),
      identityId: (() { final guardedValue = map['identityId']; if (guardedValue == null) return null; return pulumi.Input.fromValue(guardedValue as String); })(),
      lockDuration: (() { final guardedValue = map['lockDuration']; if (guardedValue == null) return null; return pulumi.Input.fromValue(guardedValue as String); })(),
      maxDeliveryCount: (() { final guardedValue = map['maxDeliveryCount']; if (guardedValue == null) return null; return pulumi.Input.fromValue((guardedValue as num).toInt()); })(),
      notifications: (() { final guardedValue = map['notifications']; if (guardedValue == null) return null; return pulumi.Input.fromValue(guardedValue as bool); })(),
      sasTtl: (() { final guardedValue = map['sasTtl']; if (guardedValue == null) return null; return pulumi.Input.fromValue(guardedValue as String); })(),
    );
  }
}
