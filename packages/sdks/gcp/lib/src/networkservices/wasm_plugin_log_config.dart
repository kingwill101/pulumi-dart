// ignore_for_file: unused_element, unnecessary_cast

import 'package:pulumi/pulumi.dart' as pulumi;

class WasmPluginLogConfig {
  /// Optional. Specifies whether to enable logging for activity by this plugin.
  final pulumi.Input<bool>? enable;

  /// Non-empty default. Specificies the lowest level of the plugin logs that are exported to Cloud Logging. This setting relates to the logs generated by using logging statements in your Wasm code.
  /// This field is can be set only if logging is enabled for the plugin.
  /// If the field is not provided when logging is enabled, it is set to INFO by default.
  /// Possible values are: `LOG_LEVEL_UNSPECIFIED`, `TRACE`, `DEBUG`, `INFO`, `WARN`, `ERROR`, `CRITICAL`.
  final pulumi.Input<String>? minLogLevel;

  /// Non-empty default. Configures the sampling rate of activity logs, where 1.0 means all logged activity is reported and 0.0 means no activity is reported.
  /// A floating point value between 0.0 and 1.0 indicates that a percentage of log messages is stored.
  /// The default value when logging is enabled is 1.0. The value of the field must be between 0 and 1 (inclusive).
  /// This field can be specified only if logging is enabled for this plugin.
  final pulumi.Input<double>? sampleRate;

  /// Creates a new [WasmPluginLogConfig].
  /// [enable] Optional. Specifies whether to enable logging for activity by this plugin.
  /// [minLogLevel] Non-empty default. Specificies the lowest level of the plugin logs that are exported to Cloud Logging. This setting relates to the logs generated by using logging statements in your Wasm code.
  /// [sampleRate] Non-empty default. Configures the sampling rate of activity logs, where 1.0 means all logged activity is reported and 0.0 means no activity is reported.
  WasmPluginLogConfig({this.enable, this.minLogLevel, this.sampleRate});

  Map<String, dynamic> toMap() {
    return <String, dynamic>{
      'enable': ?enable,
      'minLogLevel': ?minLogLevel,
      'sampleRate': ?sampleRate,
    };
  }

  factory WasmPluginLogConfig.fromMap(Map<String, dynamic> map) {
    return WasmPluginLogConfig(
      enable: (() {
        final guardedValue = map['enable'];
        if (guardedValue == null) return null;
        return pulumi.Input.fromValue(guardedValue as bool);
      })(),
      minLogLevel: (() {
        final guardedValue = map['minLogLevel'];
        if (guardedValue == null) return null;
        return pulumi.Input.fromValue(guardedValue as String);
      })(),
      sampleRate: (() {
        final guardedValue = map['sampleRate'];
        if (guardedValue == null) return null;
        return pulumi.Input.fromValue(guardedValue as double);
      })(),
    );
  }
}
