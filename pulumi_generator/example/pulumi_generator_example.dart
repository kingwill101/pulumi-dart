void main() {
  // Run this package's builder with:
  //   dart run build_runner build
  //
  // Resource libraries that define classes extending `Resource` and providing
  // `_resourceType` constants will get companion `.pulumi.dart` registration
  // extensions.
  print(
    'pulumi_generator example: run build_runner to generate .pulumi.dart files.',
  );
}
