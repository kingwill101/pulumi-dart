# Upstream integration test names intentionally not yet ported to Dart.
# One exact test name per line.
#
# Keep this file empty by default and prefer explicit skipped tests in
# integration_tests when a known upstream parity gap cannot yet be executed.
